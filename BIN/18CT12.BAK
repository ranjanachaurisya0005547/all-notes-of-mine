#include<stdio.h>
#include<conio.h>
double getGst();
void main()
{
 double r1,r2,r3,total;
 clrscr();

 r1=getGst();
 r2=getGst();
 r3=getGst();

 total=r1+r2+r3;
 printf("Total Price=%lf",total);
 getch();
}
double getGst()
{
  double p,gst;
  printf("Enter the Price:");
  scanf("%lf",&p);

  if(p>=1&&p<=1000)
  {
     gst=(p*5)/100;
  }
  else if(p>=1001&&p<=5000)
  {
     gst=(p*10)/100;
  }
  else if(p>=5001)
  {
    gst=(p*15)/100;
  }
  p=p+gst;
  return p;

}