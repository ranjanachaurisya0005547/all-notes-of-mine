//WAP to enter full name and print sort name
#include<stdio.h>
#include<conio.h>
void main()
{
  char fullname[50],fname[50];
  int i;
  clrscr();

  printf("Enter Your Full Name:");
  gets(fullname);

  for(i=0;fullname[i]!=' ';i++)
  {
       fname[i]=fullname[i];
  }
  fname[i]='\0';
  printf("First Name= %s",fname);

  getch();
}