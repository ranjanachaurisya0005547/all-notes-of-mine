//WAP to find second largest number of array between 10 array element
#include<stdio.h>
#include<conio.h>
void main()
{
 int a[10],i,j,temp=0,tmp=0;
 clrscr();

 printf("Enter 10 Array Element:");
 for(i=0;i<10;i++)
  scanf("%d",&a[i]);

 temp=a[0];
 for(i=1;i<10;i++)
 {
     if(temp>a[i])
     {
       tmp=a[i];
       for(j=2;j<10;j++)
       {
	if(tmp<a[j])
	{
	    tmp=a[j];
	    printf("%d\t",tmp);
	}
       }
     }
     else
     {
	temp=a[i];
     }

 }
 printf("Second Largest Number= %d",tmp);

 getch();
}