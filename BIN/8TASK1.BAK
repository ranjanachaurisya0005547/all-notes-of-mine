//WAP To find sum of first n numbers by using for loop
#include<stdio.h>
#include<conio.h>
void main()
{
   long int i,range,sum=0;
   clrscr();
   printf("Enter the maiximum range of number:");
   scanf("%ld",&range);

   for(i=1;i<=range;i++)
   {
      sum=sum+i;
   }
    printf("Sum of first %ld numbers is=%ld",sum);
	
   getch();
}